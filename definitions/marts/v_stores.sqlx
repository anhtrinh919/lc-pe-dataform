-- definitions/marts/v_stores.sqlx
config { type: "view", schema: "marts", name: "v_stores", tags: ["marts","dim"] }

select
  store_id,
  store_name,
  district,
  province,
  lat,
  lon
from ${ref("stage","dim_store")}