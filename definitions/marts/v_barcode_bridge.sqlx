config {
  type: "view",
  schema: "marts",
  tags: ["bridge", "barcode", "sku", "agent"]
}

/*
Grain: article_code × barcode
Purpose: Lightweight mapping bridge for AI agent queries
Note: Enrichment fields (promo/sales descriptions) removed for performance
*/

SELECT
  article_code,
  barcode,
  first_seen,
  last_seen,
  total_hits,
  mapping_method,
  is_active_recent
FROM ${ref("bridge_product_barcode")}